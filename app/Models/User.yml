definitions:
  User:
    type: object
    properties:
      id:
        type: uint
      name:
        type: string
      email:
        type: string
      password:
        type: string
    required:
      - name
      - email
      - password
